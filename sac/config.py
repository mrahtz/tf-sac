default_config = dict(
    batch_size=100,
    gamma=0.99,
    temperature=0.2,
    polyak_coef=0.995,
    n_start_env_steps=10_000,
    log_every_n_steps=100,
    checkpoint_every_n_steps=1000,
    test_every_n_steps=5000,
    async_test=False,
    buffer_size=int(1e6),
    lr=1e-3,
    env_id='Pendulum-v0',
    train_n_steps=1e5,
    seed=0,
    policy_std_min=1e-4,
    policy_std_max=4,
    network=[(400, 'relu'), (300, 'relu')]
)
